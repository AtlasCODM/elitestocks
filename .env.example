# Set to true to show the maintenance page across the application.
# Set to false (or omit) to run the normal application.
VITE_MAINTENANCE_MODE=false

# Set to true to enable the authenticated in-app support bubble and admin inbox.
VITE_FEATURE_IN_APP_SUPPORT=false
